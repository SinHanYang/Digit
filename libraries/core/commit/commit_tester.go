package commit

func main() {
	/* cg := NewCommitGraph()
	var hash = "hash2"
	t2 := time.Now()
	time.Sleep(time.Second)
	t := time.Now()
	var hash2 = "hash"
	cg.NewCommit(t2, "harry", hash2, "value", "second")
	cg.NewCommit(t, "head_author", hash, "value", "first")
	var ans = cg.GetCommit(hash)
	fmt.Println("ans:", ans.time)
	var commit = cg.GetHeadCommit()
	fmt.Println("head:", commit.author)
	var list = cg.ListCommits()
	for _, s := range list {
		fmt.Println(s.message)
	} */
}
